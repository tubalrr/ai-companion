const key="aiProjects";let projects=JSON.parse(localStorage.getItem(key)||"[]");const list=document.getElementById("projects");function esc(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}function render(){list.innerHTML=projects.length?projects.map((p,i)=>'<div class="project"><div><b>'+esc(p.name)+'</b><small>Project '+(i+1)+'</small></div><button data-i="'+i+'">Delete</button></div>').join(""):"<p>No projects yet.</p>";list.querySelectorAll("button").forEach(b=>b.onclick=()=>{projects.splice(+b.dataset.i,1);save()})}function save(){localStorage.setItem(key,JSON.stringify(projects));render()}document.getElementById("createProject").onclick=()=>{const v=document.getElementById("projectName").value.trim();if(v){projects.push({name:v});document.getElementById("projectName").value="";save()}};render();